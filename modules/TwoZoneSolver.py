class TwoZoneSolver:
	def __init__(self, ri, ro, kdry, kmoist, Ti, To, power):
		self.ri = 0
		self.ro = 0
		self.Ti = 0
		self.To = 0
		self.kdry = 0
		self.kmoist = 0

	def calculate_min_temp(self):
		print('min temp solver')

	def calculate_max_temp(self):
		print('max temp solver')